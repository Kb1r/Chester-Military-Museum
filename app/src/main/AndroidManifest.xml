<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.chestermilitarymuseum">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChesterMilitaryMuseum"
        tools:targetApi="31">

        <!-- Launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Core Activities -->
        <activity android:name=".MainActivity" android:exported="true" />
        <activity android:name=".MapActivity" />
        <activity android:name=".ContactFormActivity" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".IntroductionInfoActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".NewsActivity" />

        <!-- Info Pages -->
        <activity android:name=".SeventeenthCenturyInfoActivity" />
        <activity android:name=".EighteenthCenturyInfoActivity" />
        <activity android:name=".NineteenthCenturyInfoActivity" />
        <activity android:name=".TurnOfTheCenturyInfoActivity" />
        <activity android:name=".WW1RemembranceInfoActivity" />
        <activity android:name=".WW2AndPostWarInfoActivity" />

    </application>

</manifest>
