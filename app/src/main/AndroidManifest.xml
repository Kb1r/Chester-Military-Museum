<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chestermilitarymuseum">

    <!-- Permissions if needed -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChesterMilitaryMuseum">

        <!-- 1) Your sole LAUNCHER -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 2) All other screens, non-exported by default -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".MapActivity"
            android:exported="false" />

        <activity
            android:name=".ContactFormActivity"
            android:exported="false" />

        <activity
            android:name=".WebViewActivity"
            android:exported="false" />

        <activity
            android:name=".IntroductionInfoActivity"
            android:exported="false" />

        <activity
            android:name=".NewsActivity"
            android:exported="false" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- Info Pages -->
        <activity
            android:name=".SeventeenthCenturyInfoActivity"
            android:exported="false" />

        <activity
            android:name=".EighteenthCenturyInfoActivity"
            android:exported="false" />

        <activity
            android:name=".NineteenthCenturyInfoActivity"
            android:exported="false" />

        <activity
            android:name=".TurnOfTheCenturyInfoActivity"
            android:exported="false" />

        <activity
            android:name=".WW1RemembranceInfoActivity"
            android:exported="false" />

        <activity
            android:name=".WW2AndPostWarInfoActivity"
            android:exported="false" />

    </application>
</manifest>
